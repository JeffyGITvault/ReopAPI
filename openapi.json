{
  "openapi": "3.1.0",
  "info": {
    "title": "SECAPI",
    "version": "4.3.8",
    "description": "Fetch the latest 10-Q filings for any public company using CIK resolution, alias mapping, and SEC EDGAR integration. Returns validated SEC HTML links for GPT-compatible use."
  },
  "x-openai-function": {
    "name": "getQuarterlies",
    "description": "Retrieve the most recent 10-Q filings for a public company. Supports alias resolution, validated filing URLs, and optional caching."
  },
  "servers": [
    {
      "url": "https://secapi.5s5x.onrender.com",
      "description": "Primary Render deployment of SECAPI"
    }
  ],
  "paths": {
    "/get_quarterlies/{company_name}": {
      "get": {
        "operationId": "getQuarterlies",
        "summary": "Retrieve the most recent 10-Q filings for a public company",
        "parameters": [
          {
            "name": "company_name",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string"
            },
            "description": "Company name or ticker symbol (e.g., RH, AAPL, Meta)"
          },
          {
            "name": "count",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "default": 2,
              "minimum": 1,
              "maximum": 4
            },
            "description": "Number of recent 10-Q filings to return (max 4)"
          }
        ],
        "responses": {
          "200": {
            "description": "Structured response with CIK and validated 10-Q filing links",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "company_name": {
                      "type": "string"
                    },
                    "cik": {
                      "type": "string"
                    },
                    "filings": {
                      "type": "array",
                      "items": {
                        "$ref": "#/components/schemas/Filing"
                      }
                    },
                    "cached_quarterlies": {
                      "type": "array",
                      "description": "Reserved for future use",
                      "items": {
                        "$ref": "#/components/schemas/Filing"
                      }
                    },
                    "error": {
                      "type": "string",
                      "description": "Optional error string if resolution or retrieval failed"
                    },
                    "note": {
                      "type": "string",
                      "description": "Optional note when no filings are found"
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "Filing": {
        "type": "object",
        "properties": {
          "filing_date": {
            "type": "string",
            "format": "date"
          },
          "html_url": {
            "type": "string"
          },
          "html_link": {
            "type": "string",
            "description": "Markdown-formatted version of the HTML report"
          },
          "status": {
            "type": "string"
          },
          "display_index": {
            "type": "string"
          },
          "marker": {
            "type": "string"
          }
        }
      }
    }
  }
}
